<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8"/>
        <title>AdminSphere - Users</title>
        <style>
            body {
                font-family: Arial, Helvetica, sans-serif;
                font-size: 12px;
                color: #111;
            }

            .header {
                display: flex;
                justify-content: space-between;
                align-items: center;
                border-bottom: 2px solid #222;
                padding-bottom: 10px;
                margin-bottom: 20px;
            }
    
            .company {
                font-size: 18px;
                font-weight: bold;
            }
    
            .report-title {
                font-size: 16px;
            }
    
            .date {
                font-size: 12px;
                color: #555;
            }
    
            table {
                width: 100%;
                border-collapse: collapse;
                margin-top: 20px;
            }
    
            th {
                background-color: #f0f0f0;
                text-align: left;
                padding: 8px;
                border: 1px solid #ccc;
            }
    
            td {
                padding: 8px;
                border: 1px solid #ccc;
            }
    
            tr:nth-child(even) {
                background-color: #fafafa;
            }
    
            .footer {
                margin-top: 30px;
                font-size: 11px;
                color: #666;
                text-align: center;
            }
        </style>
    </head>
    <body>
        <div class="header">
            <div>
                <div class="company">AdminSphere</div>
                <div class="report-title">Users Report</div>
            </div>
            <div class="date">
                Generated on <%= new Date().toLocaleDateString() %>
            </div>
        </div>
        <table>
            <thead>
                <tr>
                    <th>#</th>
                    <th>Full Name</th>
                    <th>Email</th>
                    <th>Username</th>
                    <th>Role</th>
                    <th>Joined</th>
                </tr>
            </thead>
            <tbody>
                <% users.forEach((user, index) => { %>
                    <tr>
                        <td><%= index + 1 %></td>
                        <td><%= user.firstName %> <%= user.lastName %></td>
                        <td><%= user.email %></td>
                        <td><%= user.username %></td>
                        <td><%= user.role %></td>
                        <td>
                            <%= new Date(user.createdAt).toLocaleDateString() %>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
        <div class="footer">
            This report was generated by AdminSphere
        </div>
    </body>
</html>